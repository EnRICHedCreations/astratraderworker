import {readFileSync,writeFileSync,renameSync,mkdirSync} from 'node:fs';
import {dirname} from 'node:path';
import {setTimeout as sleep} from 'node:timers/promises';
import {poll,relay} from './worker.mjs';
const CHECKPOINT_MIN_INTERVAL_MS=5000;
function save(path,value){mkdirSync(dirname(path),{recursive:true});writeFileSync(path+'.tmp',JSON.stringify(value),{mode:0o600});renameSync(path+'.tmp',path)}
function supabaseState(url,key,checkpointKey,fetcher=fetch){const endpoint=new URL('/rest/v1/astratrader_meta',url),headers={apikey:key,authorization:'Bearer '+key,'content-type':'application/json'};let lastSavedAt=0,lastEncoded='';return{async load(){for(const metaKey of [checkpointKey,'worker:checkpoint']){const u=new URL(endpoint);u.searchParams.set('key','eq.'+metaKey);u.searchParams.set('select','value');const r=await fetcher(u,{headers,signal:AbortSignal.timeout(15000),redirect:'error'});if(!r.ok)throw Error('Supabase checkpoint read HTTP '+r.status);const rows=await r.json();if(rows[0]){lastEncoded=String(rows[0].value??'');lastSavedAt=Date.now();return JSON.parse(rows[0].value)}}return{}},async save(value){const encoded=JSON.stringify(value),now=Date.now();if(encoded===lastEncoded||now-lastSavedAt<CHECKPOINT_MIN_INTERVAL_MS)return;const u=new URL(endpoint);u.searchParams.set('on_conflict','key');const r=await fetcher(u,{method:'POST',headers:{...headers,Prefer:'resolution=merge-duplicates,return=minimal'},body:JSON.stringify({key:checkpointKey,value:encoded}),signal:AbortSignal.timeout(15000),redirect:'error'});if(!r.ok)throw Error('Supabase checkpoint write HTTP '+r.status);lastEncoded=encoded;lastSavedAt=Date.now()}}}
export function websocketUrl(rpcURL,explicit){if(explicit)return explicit;const u=new URL(rpcURL);u.protocol=u.protocol==='https:'?'wss:':'ws:';return u.toString()}
export function subscriptionRequests(programs,commitment='confirmed'){return programs.map((program,index)=>({jsonrpc:'2.0',id:index+1,method:'logsSubscribe',params:[{mentions:[program]},{commitment}]}))}
export function notificationSignature(message){try{const j=typeof message==='string'?JSON.parse(message):message;if(j?.method!=='logsNotification')return null;const value=j?.params?.result?.value;if(!value||value.err||typeof value.signature!=='string')return null;return value.signature}catch{return null}}
async function heartbeat(url,token,data){const r=await fetch(new URL('/webhooks/solana/heartbeat',url),{method:'POST',headers:{'content-type':'application/json',authorization:'Bearer '+token},body:JSON.stringify(data),signal:AbortSignal.timeout(10000),redirect:'error'});if(!r.ok)throw Error('Heartbeat HTTP '+r.status)}
function startStream({wsURL,programs,onSignature,enabled=true,onEvent=()=>{}}){let stopped=false,socket=null,retryMs=1000,connected=false,reconnectTimer=null;const seen=new Map(),remember=sig=>{const now=Date.now();if(seen.has(sig))return false;seen.set(sig,now);if(seen.size>5000){const cutoff=now-1800000;for(const [key,at]of seen){if(at<cutoff||seen.size>4000)seen.delete(key);else break}}return true},schedule=()=>{if(stopped||!enabled)return;clearTimeout(reconnectTimer);reconnectTimer=setTimeout(connect,retryMs);retryMs=Math.min(30000,retryMs*2)},connect=()=>{if(stopped||!enabled)return;try{socket=new WebSocket(wsURL)}catch{schedule();return}socket.addEventListener('open',()=>{connected=true;retryMs=1000;for(const request of subscriptionRequests(programs,'confirmed'))socket.send(JSON.stringify(request));console.log(JSON.stringify({at:new Date().toISOString(),event:'stream_connected',programs:programs.length}))});socket.addEventListener('message',event=>{const sig=notificationSignature(String(event.data));if(sig&&remember(sig)){onEvent(Date.now());onSignature(sig)}});socket.addEventListener('error',()=>{});socket.addEventListener('close',()=>{connected=false;if(!stopped)schedule()})};if(enabled)connect();return{stop(){stopped=true;clearTimeout(reconnectTimer);try{socket?.close()}catch{}},get connected(){return connected}}}
async function main(){const e=process.env,url=e.ASTRATRADER_URL,token=e.INGEST_TOKEN,rpcURL=e.SOLANA_RPC_URL,backend=e.PERSISTENCE_BACKEND??(e.SUPABASE_URL?'supabase':'file'),path=e.WORKER_STATE_PATH,usingSupabase=backend==='supabase',sampleSize=Math.max(1,Math.min(100,Number(e.DISCOVERY_SAMPLE_SIZE??50)||50)),pollMs=Math.max(250,Math.min(5000,Number(e.DISCOVERY_POLL_MS??1000)||1000)),heartbeatMs=Math.max(5000,Math.min(30000,Number(e.PIPELINE_HEARTBEAT_MS??10000)||10000)),rpcTimeoutMs=Math.max(1000,Math.min(10000,Number(e.REALTIME_RPC_TIMEOUT_MS??5000)||5000)),streaming=(e.STREAMING_DISCOVERY_ENABLED??'true')==='true',legacyConfirmedPoll=(e.LEGACY_CONFIRMED_POLLING??'false')==='true',wsURL=websocketUrl(rpcURL,e.SOLANA_WS_URL);const missing=[['ASTRATRADER_URL',url],['INGEST_TOKEN',token?.length>=32],['SOLANA_RPC_URL',rpcURL],['PERSISTENCE_BACKEND',['file','supabase'].includes(backend)],['WORKER_STATE_PATH',usingSupabase||path],['SUPABASE_URL',!usingSupabase||e.SUPABASE_URL],['SUPABASE_SERVICE_ROLE_KEY',!usingSupabase||e.SUPABASE_SERVICE_ROLE_KEY?.length>=20],['PERSISTENCE_CONFIRMED',e.PERSISTENCE_CONFIRMED==='true']].filter(x=>!x[1]).map(x=>x[0]);if(missing.length)throw Error('Configure realtime worker environment: '+missing.join(', '));const remote=usingSupabase?supabaseState(e.SUPABASE_URL,e.SUPABASE_SERVICE_ROLE_KEY,'worker:checkpoint:realtime'):null;let state={};if(remote)state=await remote.load();else try{state=JSON.parse(readFileSync(path,'utf8'))}catch(error){if(error.code!=='ENOENT')throw error}const persist=remote?(s=>remote.save(s)):(s=>save(path,s)),programs=(e.DISCOVERY_PROGRAMS??'JUP6LkbZbjS1jKKwapdHNy74zcZ3tLUZoi5QNyVTaV4').split(',').map(x=>x.trim()).filter(Boolean);let stop=false,cycle=0,streamQueued=0,streamErrors=0,streamChain=Promise.resolve(),lastConfirmedStreamEventAt=0,lastFinalizedReconciliationAt=0,lastHeartbeatError='',rpcCalls=0,rpcErrors=0,rpcTimeouts=0,rpcLastMs=0,rpcMaxMs=0,rpcTotalMs=0;const enqueueStream=signature=>{streamChain=streamChain.then(()=>relay([signature],url,token,fetch,'confirmed')).then(()=>streamQueued++).catch(()=>streamErrors++)},stream=startStream({wsURL,programs,onSignature:enqueueStream,enabled:streaming,onEvent:at=>lastConfirmedStreamEventAt=at});let heartbeatBusy=false;const rpcStats=()=>({rpcCalls,rpcErrors,rpcTimeouts,rpcLastMs,rpcMaxMs,rpcAvgMs:rpcCalls?Math.round(rpcTotalMs/rpcCalls):0,rpcTimeoutMs});const sendHeartbeat=async()=>{if(stop||heartbeatBusy)return;heartbeatBusy=true;try{await heartbeat(url,token,{streaming,streamConnected:stream.connected,lastConfirmedStreamEventAt,lastFinalizedReconciliationAt,...rpcStats()});lastHeartbeatError=''}catch(error){lastHeartbeatError=String(error?.message??error).slice(0,160);console.error(JSON.stringify({at:new Date().toISOString(),event:'heartbeat_failed',error:lastHeartbeatError}))}finally{heartbeatBusy=false}},heartbeatTimer=setInterval(()=>void sendHeartbeat(),heartbeatMs);heartbeatTimer.unref?.();const shutdown=()=>{stop=true;clearInterval(heartbeatTimer);stream.stop()};process.on('SIGTERM',shutdown);process.on('SIGINT',shutdown);await sendHeartbeat();const rpc=async(method,params)=>{const started=Date.now();rpcCalls++;try{const r=await fetch(rpcURL,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({jsonrpc:'2.0',id:1,method,params}),signal:AbortSignal.timeout(rpcTimeoutMs),redirect:'error'});if(!r.ok)throw Error('RPC HTTP '+r.status);const j=await r.json();if(j.error)throw Error('RPC code '+j.error.code);if(!Array.isArray(j.result))throw Error('Unexpected RPC response');return j.result}catch(error){rpcErrors++;if(error?.name==='TimeoutError'||error?.name==='AbortError')rpcTimeouts++;throw error}finally{rpcLastMs=Date.now()-started;rpcTotalMs+=rpcLastMs;rpcMaxMs=Math.max(rpcMaxMs,rpcLastMs)}};while(!stop){const started=Date.now();try{let chain=Promise.resolve();const persistSerialized=()=>{chain=chain.then(()=>persist(state));return chain},jobs=[];for(const program of programs){jobs.push(poll(program,state,rpc,(items,c)=>relay(items,url,token,fetch,c),persistSerialized,sampleSize,'finalized','finalized'));if(legacyConfirmedPoll)jobs.push(poll(program,state,rpc,(items,c)=>relay(items,url,token,fetch,c),persistSerialized,sampleSize,'confirmed','confirmed'))}const counts=await Promise.all(jobs);await chain;lastFinalizedReconciliationAt=Date.now();cycle++;console.log(JSON.stringify({at:new Date().toISOString(),event:'realtime_checkpoint',streamConnected:stream.connected,streamQueued,streamErrors,finalizedQueued:counts.reduce((a,b)=>a+b,0),cycle,cycleMs:Date.now()-started,heartbeatError:lastHeartbeatError||null,...rpcStats()}))}catch(error){console.error(JSON.stringify({at:new Date().toISOString(),event:'realtime_failed',error:String(error?.message??error).slice(0,160),...rpcStats()}));await sleep(500)}const remaining=pollMs-(Date.now()-started);if(remaining>0)await sleep(remaining)}clearInterval(heartbeatTimer);await streamChain}
main().catch(e=>{console.error(e.message);process.exitCode=1});
